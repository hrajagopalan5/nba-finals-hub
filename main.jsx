import React from "react";
import ReactDOM from "react-dom/client";
import NBAFinalsHub from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NBAFinalsHub />
  </React.StrictMode>
);
